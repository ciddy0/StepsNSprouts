/**
 * lookup table to differentiate different kinds of items in a user's inventory. 
 * (Decoration, Tree Theme, Garden Theme, etc.)
 */
export interface ItemType {
    id: string;
    name: string;
}